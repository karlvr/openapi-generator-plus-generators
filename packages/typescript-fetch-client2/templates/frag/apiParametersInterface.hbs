export interface {{{className (concat name '_' 'request')}}} {
{{#each parameters}}
	{{>frag/propertyDocumentation memberOf=null}}
	{{name}}{{#unless required}}?{{/unless}}: {{{nativeType}}}
{{/each}}
}

