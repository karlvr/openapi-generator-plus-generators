{{#if discriminator}}
{{#with discriminator}}
	/**
	 * Discriminator property
	 */
	{{serializedName}}: {{{nativeType.serializedType}}};
	
{{/with}}
{{/if}}
{{#each discriminatorValues}}
	/**
	 * Value for discriminator in {{#each schemas}}{{nativeType.parentType}}{{#hasMore}}, {{/hasMore}}{{/each}}
	 */
	{{discriminator.serializedName}}: {{{literalValue}}};

{{/each}}
