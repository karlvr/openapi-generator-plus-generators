{{#if @root.includePolyfills}}
import "whatwg-fetch";
{{/if}}
export const defaultFetch = (typeof window !== "undefined" && typeof window.fetch !== "undefined" ? window.fetch.bind(window) : typeof fetch !== "undefined" ? fetch : undefined);
