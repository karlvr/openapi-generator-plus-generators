{{>frag/schemaDocumentation}}
export type {{className name}} =
{{#each enumValues}}
	{{{literalValue}}}{{#hasMore}} |{{/hasMore}}
{{/each}}

export namespace {{className name}} {
	export enum Enum {
	{{#each enumValues}}
		{{{name}}} = {{{literalValue}}}{{#hasMore}},{{/hasMore}}
	{{/each}}
	}
}
