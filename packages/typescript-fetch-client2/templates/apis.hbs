{{>index}}

{{#each groups}}
export { default as {{className name}}Api, paramCreator as {{className name}}ApiParamCreator, withConfiguration as {{identifier name}}WithConfiguration } from "./api/{{identifier name}}";
{{/each}}
