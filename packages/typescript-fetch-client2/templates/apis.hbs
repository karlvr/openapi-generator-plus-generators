{{>index}}

{{#each groups}}
export { default as {{className name}}Api, paramCreator as {{className name}}ApiParamCreator, apiFactory as {{className name}}ApiFactory, apiFunction as {{className name}}ApiFp, {{className name}}ApiInterface } from "./api/{{identifier name}}";
{{/each}}
