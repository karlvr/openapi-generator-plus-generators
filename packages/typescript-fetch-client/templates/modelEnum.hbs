/**
{{#if description}}
 * {{{description}}}
{{/if}}
 * @export
 * @enum {string}
 */
export enum {{className name}} {
{{#each enumValues}}
	{{{name}}} = {{{literalValue}}}{{#hasMore}},{{/hasMore}}
{{/each}}
}
