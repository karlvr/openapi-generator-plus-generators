package {{invokerPackage}};

/**
 * The implementation of this interface provides access to the api groups available in this client API.
 */
{{>generatedAnnotation}}
public interface ClientApi {
	
	{{#each apis}}
	{{@root.apiPackage}}.{{className name}}Api get{{className name}}Api();
	{{@root.apiPackage}}.{{className name}}Api new{{className name}}Api();

	{{/each}}

}
