<span class="model-property-type">
	<section class="model-array-items">
		<div class="model-inner-schema">
			enum[]
    		<span class="model-property-enum" title="Possible values">
        		&isin; 
        		{
					{{~#each enum~}}
					    <span class="model-property-enum-item"><a class="model-ref" href="#/definitions/{{.}}">{{model--subschema-name .}}</a></span>{{#unless @last}}, {{/unless}}
					{{~/each~}}
		        }
    		</span>
    	</div>
    </section>
</span>
