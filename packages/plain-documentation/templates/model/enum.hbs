<div class="values -enum">
	<h4>Values</h4>
	<span class="model-property-enum" title="Constant">
		[
		{{#each enumValues}}
			<span class="model-property-enum-item" title="{{literalValue}}">{{name}}</span>{{#hasMore}},{{/hasMore}}
		{{/each}}
		]
	</span>
</div>
