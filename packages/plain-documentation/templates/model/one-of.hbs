{{!
renders a model "allOf"-definition.
}}
<div class="one-of">
    <h5>One of</h5>
    <div class="detail">
        {{#each composes}}
            <p><a class="model-ref" href="#{{../anchor}}/{{htmlId nativeType}}">{{nativeType}}</a></p>
        {{/each}}
    </div>

    {{#if discriminator}}
    {{>model/discriminator}}
    {{/if}}
</div>
