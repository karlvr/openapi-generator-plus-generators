{{!--
    Renders the request-body section of an operation.
--}}

{{#bodyParam}}
    <div class="request">
        <h5>Request</h5>
            {{{md description}}}

            <p class="meta">{{>model/datatype anchor='/definitions'}}</p>

            {{#if ../consumes}}
            <p>{{#each ../consumes}}<span class="type">{{mediaType}}</span> {{/each}}</p>
            {{/if}}
    </div>
{{/bodyParam}}
