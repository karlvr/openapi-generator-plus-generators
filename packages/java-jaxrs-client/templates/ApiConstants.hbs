package {{apiPackage}};

{{>generatedAnnotation}}
public interface ApiConstants {

	{{#if server.description}}
	/**
	 * {{{md server.description}}}
	 */
	{{/if}}
	java.lang.String DEFAULT_SERVER = {{{stringLiteral server.url}}};

	{{#each servers}}
	{{#if description}}
	/**
	 * {{{md description}}}
	 */
	{{/if}}
	{{#if vendorExtensions.x-name}}
	java.lang.String {{constantName (concat vendorExtensions.x-name 'Server')}} = {{{stringLiteral url}}};
	{{else}}
	java.lang.String {{constantName (concat 'Server' '_' @index)}} = {{{stringLiteral url}}};
	{{/if}}

	{{/each}}
	{{>hooks/apiConstantsBody}}
}
