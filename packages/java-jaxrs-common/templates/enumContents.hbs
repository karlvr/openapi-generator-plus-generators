{{#each enumValues}}
@javax.xml.bind.annotation.XmlEnumValue({{{literalValue}}}) {{{name}}}({{{literalValue}}}){{#unless @last}}, {{else}};{{/unless}}
{{/each}}

private {{enumValueNativeType}} value;

{{className name}}({{enumValueNativeType}} v) {
	value = v;
}

@com.fasterxml.jackson.annotation.JsonValue
public String value() {
	return value;
}

@Override
public String toString() {
	return String.valueOf(value);
}

public static {{className name}} fromValue(String v) {
	for ({{className name}} b : {{className name}}.values()) {
		if (String.valueOf(b.value).equals(v)) {
			return b;
		}
	}
	throw new java.lang.IllegalArgumentException("Unsupported {{className name}} value: " + v);
}
