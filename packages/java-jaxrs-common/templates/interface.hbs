package {{modelPackage}};

{{#with interface}}
{{#if description}}
/**
 * {{description}}
 */
{{/if}}
{{>generatedAnnotation}}
{{>frag/pojoHeader}}
public interface {{className name}} {{>interfaceExtends}} {

	{{>nestedModels}}
	{{>interfaceContents}}
}
{{/with}}
